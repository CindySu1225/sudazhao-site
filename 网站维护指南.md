# 网站维护指南 · 苏大朝作品集

这份清单写给日常维护这个网站的人。看不懂技术细节也没关系，照着步骤做即可。

- **网站地址**：https://www.sudazhao.com （DNS 生效后）
- **临时地址**：https://cindysu1225.github.io/sudazhao-site/
- **代码仓库**：https://github.com/CindySu1225/sudazhao-site
- **项目文件夹**：`/Users/cindysu/Documents/Dachao`

---

## 0. 核心概念（只需记住一句话）

> **改文件 → 推送（push）→ 等 1~2 分钟 → 网站自动更新。**

域名一次配好后永远不用再动。以后所有改动只是「改文件 + 推送」。

---

## 1. 本地预览（改之前先在自己电脑上看效果）

打开「终端」（Terminal），进入项目文件夹并启动预览：

```bash
cd /Users/cindysu/Documents/Dachao
npm run dev
```

然后浏览器打开 **http://localhost:4321/** 就能看到。
改文件时页面会自动刷新。看完按 `Ctrl + C` 停止。

> 线上用户完全看不到你在本地的改动，放心试。

---

## 2. 加一幅新作品

每幅作品 = **1 张图片 + 1 个说明文件**，都放在
`src/content/artworks/` 文件夹里。

### 步骤

1. 把作品图片放进 `src/content/artworks/`，建议命名带序号，例如 `18-新作品.jpeg`。
2. 在同一文件夹新建一个同名的 `.md` 文件，例如 `18-新作品.md`，内容照抄下面模板：

```markdown
---
title: "作品名称"
image: "./18-新作品.jpeg"
year: 2026
medium: "iPad绘画"
size: "30 x 40 cm"
order: 18
featured: false
---
```

### 各字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | ✅ | 作品名称 |
| `image` | ✅ | 图片文件名，前面加 `./`，要和实际文件名完全一致 |
| `year` | 可选 | 创作年份 |
| `medium` | 可选 | 材质，如「布面油画」「iPad绘画」 |
| `size` | 可选 | 尺寸，如「30 x 40 cm」 |
| `order` | 可选 | 排序，**数字越小越靠前**；不写默认排最后 |
| `featured` | 可选 | `true` = 设为首页大图（同一时间只让一幅为 true） |

3. 本地预览确认没问题 → 按第 5 节推送上线。

---

## 3. 改文字（简介 / 联系方式等）

| 想改的内容 | 改哪个文件 |
|-----------|-----------|
| 首页大标题、副标题 | `src/pages/index.astro` |
| 关于我（个人简介） | `src/pages/about.astro` |
| 联系方式（邮箱） | `src/pages/contact.astro` |
| 页脚 | `src/components/Footer.astro` |

在文件里找到对应的中文，直接改掉即可。改完本地预览 → 推送。

> ⚠️ **隐私提醒**：电话、家庭住址等私人信息**绝对不要**写进任何文件，整个网站和代码都是公开的。

---

## 4. 换背景图 / 调颜色

- 四个页面的背景图在 `src/assets/`：
  `home-bg.png`（首页）、`works-bg.png`（作品集）、
  `about-bg.jpeg`（关于我）、`contact-bg.jpeg`（联系方式）。
- 想换图：用同名新图覆盖，或改对应页面文件里的 `import` 文件名。
- 想调浓淡/颜色：这类改动建议直接找 Claude 帮忙，说一句「首页背景再淡一点」即可。

---

## 5. 推送上线（让改动生效）

在终端，项目文件夹下依次运行：

```bash
cd /Users/cindysu/Documents/Dachao
git add -A
git commit -m "简单描述这次改了什么，比如：新增作品《XXX》"
git push
```

推送后，GitHub 会自动构建并部署，**约 1~2 分钟**网站更新。

### 查看部署是否成功

```bash
gh run list --limit 1
```

看到 `completed  success` 就是成功了。
也可以网页查看：仓库页面 → 上方 **Actions** 标签。

---

## 6. 改错了怎么办（撤销）

- **还没推送**，想丢弃本地所有未提交的改动：
  ```bash
  git restore .
  ```
- **已经推送但想回到上一个好版本**：找 Claude 帮忙最稳妥，
  或运行（谨慎）：
  ```bash
  git revert HEAD
  git push
  ```
  这会新增一次「撤销提交」，安全地把网站退回上一版。

> 每次改动都有完整历史记录，**几乎不可能彻底改坏**，别怕动手。

---

## 7. 常见问题

**Q：推送时让我输入用户名密码？**
已用 GitHub CLI 登录，一般不会再问。若失效，运行 `gh auth login` 重新登录。

**Q：网站打不开 / 域名不通？**
先试临时地址 https://cindysu1225.github.io/sudazhao-site/ ：
- 临时地址能开、域名不能开 → DNS 问题（去 julydns 后台检查解析记录）。
- 两个都不能开 → 看 `gh run list` 最近一次部署是否失败。

**Q：国内访问慢？**
GitHub 服务器在国外，大陆访问可能偏慢，属正常现象。需要的话再加国内 CDN。

---

## 8. 最省事的办法

任何改动，**直接告诉 Claude**：
- 「加一幅作品，名字叫《XXX》，2026 年，图片我放在桌面了」
- 「把关于我第二段改成……」
- 「首页背景再淡一点」

Claude 会改好、本地验证、推送上线，和这次一样。
这份清单是给你想自己动手时备查的。
